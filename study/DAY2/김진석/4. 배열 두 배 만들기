class Solution {
    public int[] solution(int[] numbers) {
        int a = numbers.length;
        for(int i = 0; i<a ; i++){
            numbers[i] = numbers[i]*2;
        }
        int[] answer = numbers;
        return answer;
    }
}
